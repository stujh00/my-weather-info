# 제출문 초안 (C27, C28)

아래는 뼈대입니다. `[ ]` 부분은 실제로 해본 뒤 본인 말로 채워 넣으세요. AI가 대신 "판단"을 지어내면 안 되는 항목(②③)은 특히 직접 쓰세요.

## 짧은 확인 방법 (C27) — 4줄

1. **어디로 가나요**: 배포된 URL(`https://[배포주소]`)을 새 시크릿 창으로 엽니다.
2. **3단계 이내 무엇을 하나요**: ① "지금 다시 조회" 버튼 클릭 → ② 값·단위·출처·시각이 뜨는지 확인 → ③ 페이지 아래 "합성 시험 패널"에서 실패 버튼 하나를 눌러 stale 표시를 확인.
3. **무엇이 보이면 통과인가요**: 기온 값과 °C, 출처(Open-Meteo) 링크, 출처 시각·조회 시각·Asia/Seoul 표시가 한 화면에 보이고, 일별 기록 표에 서로 다른 날짜 2건이 있으면 통과입니다.
4. **안 될 때 무엇이 보이나요**: 조회가 실패하면 값이 사라지지 않고 "오래된 값(마지막 정상값)" 배지와 함께 실패 종류(예: stale · 오프라인)가 표시됩니다.

## AI와 나의 판단 (C28) — 3줄

1. **AI에게 맡긴 일**: [예: Next.js/Supabase 뼈대 코드, adapter 로직 포팅, fixture 재생 UI 초안 작성을 Claude에게 맡겼다]
2. **직접 판단한 일**: [예: 어떤 값(기온)을 추적할지, 어떤 DB(Supabase)를 쓸지, 배포 플랫폼(Vercel)을 직접 정했다. 실제 두 날짜의 조회도 본인이 직접 실행했다]
3. **AI 제안을 따르지 않은 일**: [없었다면 이유를 쓰세요. 예: "특별히 다른 방향을 검토하지 않아 없음" 또는 실제로 바꾼 부분이 있다면 그 내용]
